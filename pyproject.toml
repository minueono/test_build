[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Alcohol_Total_Game"
authors = [{name = "minueono", email = "nexcoremy@icloud.com"}]
dynamic = ["version", "description"]
